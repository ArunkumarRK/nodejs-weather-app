<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="css/style.css" />
    </head>
    <body>
        {{!-- <h1>{{title}}</h1> --}}
        <div class="main-content">
            {{> header}}
            {{errorMessage}}
        </div>
        {{> footer}}
    </body>
</html>